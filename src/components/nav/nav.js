import React from 'react';
import './style.scss';

class Nav extends React.Component {
    render() {
      return (
        <nav >
          this is where the nav bar will go 
        </nav>
      );
    }
  };

export default Nav;